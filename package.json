{
  "name": "openwright",
  "version": "0.1.0",
  "description": "BYOA slide-deck workspace: agent-built HTML decks with PDF/PPTX export. Claude, Copilot, or Codex at the core.",
  "private": true,
  "scripts": {
    "dev": "bun run src/server.ts",
    "start": "bun run src/server.ts",
    "setup": "bash setup.sh",
    "setup:win": "powershell -ExecutionPolicy Bypass -File setup.ps1"
  },
  "type": "module",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.1.13"
  },
  "bin": {
    "openwright": "./bin/openwright.ts"
  }
}